import { Helmet } from "react-helmet";

const Update = () => {
    return (
        <div>
            <Helmet>
                <title>Update-Profile</title>
            </Helmet>
            Update Profile
        </div>
    );
};

export default Update;